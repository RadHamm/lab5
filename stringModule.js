// Function to convert a string UpperCase
export function toUpperCase(str) {
    return str.toUpperCase();
}

// Function to convert a string to lowercase
export function toLowerCase(str) {
    return str.toLowerCase();
}